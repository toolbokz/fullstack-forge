# One-page Next.js — Website Sales Landing

This repository is a minimal, SEO-minded one-page site built with Next.js for selling website services.

Quick start

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
npm start
```

Notes

- Edit site metadata in `pages/index.js`.
- Place production images in `public/` and update URLs.
- Add real contact handling for the form (API route or third-party service).

# fullstack-forge
